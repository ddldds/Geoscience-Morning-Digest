name: Daily Paper Digest

on:
  schedule:
    - cron: '0 23 * * *' # 北京时间 7:00
  workflow_dispatch:

# 1. 必须添加写入权限
permissions:
  contents: write

jobs:
  digest:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        # ⚠️ 【关键修复】确保 Checkout 时能获取 Token，以支持后续的 Push 操作
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install openai requests feedparser

      - name: Run RSS Reader
        run: python scripts/rss_reader.py

      - name: Generate DeepSeek Digest
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: python scripts/generate_digest.py

      - name: Send Email
        env:
          EMAIL_USER: ${{ secrets.EMAIL_USER }}
          EMAIL_PASS: ${{ secrets.EMAIL_PASS }}
        run: python scripts/send_email.py

      # 2. 【关键修复】新增步骤：保存 seen.json 的状态回仓库
      - name: Commit and Push changes
        run: |
          # 确保 Git 配置了正确的用户信息
          git config --global user.name "GitHub Action Bot"
          git config --global user.email "action@github.com"
          
          # 检查 state/seen.json 文件是否有变动
          if [ -n "$(git status --porcelain state/seen.json)" ]; then
            git add state/seen.json
            git commit -m "Update seen papers list [skip ci]"
            
            # 🚀 显式使用 GITHUB_TOKEN 进行 Push，这是最可靠的认证方式
            git push "https://${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git" HEAD:main
            
            echo "Successfully pushed updated seen.json"
          else
            echo "No changes in seen.json to commit."
          fi
